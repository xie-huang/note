顾名思义，就是将单片机的各个模块封装成各个文件。
步骤：
1.定义.c与.h文件，然后封装硬件函数，将其定义
![](image-14.png)
![alt text](image-15.png)
2.主函数预处理阶段调用并编译，链接
![alt text](image-16.png)
过程理解
头文件在预处理里面相当于将.h文件的东西原封不动复制到里面，然后在编译阶段程序会检查调用的函数泵文件是否有，不管有没有定义，有存在就可以。之后链接阶段程序会主动去找函数的具体指令。