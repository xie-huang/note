### 本质辨析
##### 一、数组名
数组名本质是一个编辑器的只读标识，存储两个信息：首元素地址与数组成员数量等整体属性信息，在不同场景下会体现出部分不同本质。
表现有四：
1.在大部分情况下（赋值，传参，引用，指针运算），其代表的是首元素地址（即指针）
2.但其作为sizeof（）的唯一参数时，其作为数组本身这一整体属性存在，
eg：char name[10086]；
sizeof（name）；
其测出来是整个数组的长度，但如果参数不只是数组名而是其运算，那么其代表的是首元素地址，例如
eg：char name[10086]；
sizeof（name+1）；
printf（"%d",sizeof（name+1））;
输出如下：8
这是因为现在代表的是一个元素所占的内存大小
3.在&符情况下，其代表的是整个数组的地址，等于首元素地址
4.在定义时，本质为整个本质
##### 二、与指针变量差别
指针变量作为一个存储地址的变量（所以要找地址要解引用*），可以对其进行赋值，修改等变换，数组名本质是一个标识，并不占据地址，不可进行变换。
##### 三、枚举
本质是int型常量，